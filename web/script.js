function runCode() {
  const javaCode = document.getElementById('javaCode').value;
  const result = document.getElementById('result');
  result.textContent = `كود جافا الذي تم إدخاله:\n${javaCode}`;
}
